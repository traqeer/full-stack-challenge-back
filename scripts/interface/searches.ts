export interface SearchLegacy {
  term: string;
  userId: string;
  searchType: string;
  maxResults: string;
  cronInterval: string;
  createdAt: Date;
  updatedAt: Date;
  accumulatedCost: number;
}
